export const metadata = {
  title: 'Helix Core — AI built for offensive security',
  description: 'Helix Core is the intelligence engine powering every Helix interaction. Built from the ground up for security research, red teaming, and adversarial reasoning.',
}

export default function CoreLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
